# 前端请求后端 API 的基础地址（Vite 在「构建时」注入；前后端不同域名时生产环境必填）
# 本地开发通常使用 /api（配合 Vite proxy）
VITE_API_BASE_URL=/api

# 生产环境（Railway 前端服务）：在 Variables 里设置后重新触发构建（Redeploy），仅改变量不重构建不会生效
# 须以 /api 结尾，与后端 app.use('/api/...') 一致
# VITE_API_BASE_URL=https://你的后端域名.up.railway.app/api
# 后端 Variables 里 CORS_ORIGIN 需包含前端页面完整 origin（含 https://，无路径）
