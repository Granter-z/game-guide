/**
 * 前后端不同域名时，在这里写后端根地址（必须以 /api 结尾）。
 * 此文件会优先生效，可覆盖构建里错误的默认 /api。
 * 示例：window.__GAME_GUIDE_API_BASE__ = 'https://你的后端.up.railway.app/api';
 */
window.__GAME_GUIDE_API_BASE__ = 'https://game-guide-production.up.railway.app/api';
